<p id="notice"><%= notice %></p>
<h2><%= @task.name %></h2>
<p><%= @task.description %></p>
<p>[[Don't always keep this visible - API tokens may be shown]]<br /><%= @task.action %></p>
<%= link_to 'Edit', edit_task_path(@task) %> |
<%= link_to 'Back', tasks_path %>
